carrot:
  sample: '${CARROT_SAMPLE}'
  method: '${CARROT_METHOD}'
  mode: '${CARROT_MODE}'


wcmc:
  pipeline:
    workflow:
      config:
        correction:
          peak:
            intensity: 10000

logging:
  level:
    root: INFO

wcmc:
  api:
    mona:
      rest:
        server:
          user: admin
          password: admin

mona:
  rest:
    server:
      host: localhost
      port: 9090
