

mona:
  rest:
    server:
      user: admin
      password: admin
      host: localhost
      port: 9090

spring:
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379


logging:
  register-shutdown-hook: false

