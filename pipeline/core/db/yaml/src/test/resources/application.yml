logging:
  level:
    root: info

spring:
  profiles: "carrot.resource.loader.bucket.data"

carrot:
  targets:
    yaml:
      properties:
        resource: "libraries.yaml"

wcmc:
  workflow:
    resource:
      store:
        bucket:
          name: "carrot-libraries"
